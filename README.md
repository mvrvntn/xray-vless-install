# 🌐 Автоматическая установка Xray (VLESS TCP XTLS-Vision) & Hysteria 2 с поддержкой подписок, WARP и Opera Proxy

<p align="center">
  <img src="https://img.shields.io/badge/Xray-Core%20v1.8.0+-blue?style=for-the-badge&logo=linux&logoColor=white" alt="Xray Version">
  <img src="https://img.shields.io/badge/Hysteria-2--latest-cyan?style=for-the-badge" alt="Hysteria Version">
  <img src="https://img.shields.io/badge/Security-XTLS--Vision-brightgreen?style=for-the-badge" alt="Security XTLS">
  <img src="https://img.shields.io/badge/WARP-Smart%20%2F%20Full%20Routing-purple?style=for-the-badge" alt="WARP Routing">
  <img src="https://img.shields.io/badge/Opera--Proxy-OpenAI%20Bypass-orange?style=for-the-badge" alt="Opera Proxy">
</p>

Скрипт `install_xray.sh` — это полностью автоматизированное, профессиональное решение для развертывания высокопроизводительных и защищенных прокси-серверов **VLESS TCP XTLS-Vision** и **Hysteria 2** на вашем VPS. 

Скрипт разработан с упором на максимальную надежность, скорость обхода блокировок, простоту администрирования и **100% совместимость при совместной установке рядом с VPN-серверами вроде AntiZapret**.

---

## ✨ Ключевые возможности проекта

*   🚀 **Протокол VLESS TCP XTLS-Vision (Порт 443 TCP)** — золотой стандарт маскировки трафика. По умолчанию используется **надежный отпечаток TLS (`fp=ios`)**, обеспечивающий максимальную стабильность. Доступен интерактивный выбор отпечатков (включая `chrome`, `firefox`, `random` и др.). Везде принудительно используется **TLS 1.3** и чистый протокол **`alpn=http/1.1`** для 100% совместимости с любыми клиентами (Happ, Incy, Hiddify, V2rayN, Shadowrocket).
*   ⚡ **Hysteria 2 (Порт 443 UDP)** — сверхбыстрый протокол на основе UDP/QUIC, идеально подходящий для плохих и нестабильных мобильных сетей. Полностью интегрирован со всеми клиентами, UUID используется как единый логин и пароль. Автоматически опускается параметр `obfs`, если обфускация отключена, предотвращая ошибки подключения на клиентах.
*   📱 **Динамический сервер подписок (Base64)** — разворачивает локальный легковесный Python-демон подписок. Импортируйте ссылку вида `https://ваш-домен.com/sub/UUID` в любой клиент, и настройки устройств обновятся автоматически! Специально оптимизировано для клиентов **Happ** и **Incy**: автоматическое включение Anti-DPI фрагментации (`tlshello`), умный автоконнект, интервалы обновлений и нативное оформление интерфейса профилей в приложении.
*   🌀 **Умный обход блокировок через Cloudflare WARP**:
    *   **Избирательный обход (Smart - по умолчанию)**: через WARP автоматически маршрутизируются заблокированные ресурсы. Скрипт ежедневно скачивает и объединяет два популярных списка с GitHub: основной список геоблокировок РФ (`geoblock.lst`) и список сервисов Google AI (`google_ai.lst` для Gemini и др.).
        *   ✅ *Плюсы:* Максимальная скорость, нет проблем с капчами и доступом к локальным сайтам, которые блокируют IP Cloudflare.
        *   ❌ *Минусы:* Ваш серверный IP виден сайтам. Если IP вашего хостинга забанен самим сервисом, он не откроется.
    *   **Весь трафик (Full)**: весь исходящий трафик сервера оборачивается в сеть Cloudflare.
        *   ✅ *Плюсы:* Полная анонимность вашего VPS (все сайты видят "чистый" IP Cloudflare). Автоматически обходит строгие блокировки хостингов по IP-адресам.
        *   ❌ *Минусы:* Незначительно вырастает пинг. Google и другие сервисы могут чаще показывать проверку CAPTCHA ("Я не робот").
*   🌀 **Opera Proxy (для OpenAI/ChatGPT и Claude)**:
    *   **Зачем нужен:** Сервисы OpenAI (ChatGPT) и Claude активно блокируют IP-адреса хостинг-провайдеров и сетей Cloudflare WARP, возвращая ошибку `Access Denied (403)` или постоянные капчи. Opera Proxy перенаправляет запросы через резидентные (домашние) прокси-серверы Opera, которые вызывают полное доверие у систем защиты.
    *   **Синергия с WARP:** Работает одновременно с Cloudflare WARP. Запросы к ChatGPT идут через резидентные прокси, а весь остальной заблокированный трафик (Instagram, Twitter и др.) — через быстрый WARP с низким пингом.
    *   **Где настраивать:** Список доменов, которые направляются через прокси Opera, хранится в текстовом файле на сервере: `/etc/xray/opera.lst`. Вы можете добавить туда новые домены или удалить ненужные, после чего перезапустить службы через меню `xry`. Интегрирован в Xray как приоритетный socks-outbound (порт `40001`).
*   🚦 **Интеграция RoscomVPN**: для клиентов Happ и Incy сервер автоматически отдает оптимизированный роутинг.
*   🇳🇱 **Автоопределение геолокации**: скрипт определяет страну вашего VPS и автоматически добавляет соответствующий флаг-эмодзи в имя подключения (например, `🇳🇱 VLESS-TCP`).
*   ⚡ **Ядерные оптимизации (BBR + TFO + KeepAlive)**: автоматическое включение Google BBR, агрессивного KeepAlive (300с) и TCP Fast Open на уровне ядра Linux снижает пинг, ускоряет загрузку данных и предотвращает зависание 'спящих' соединений мобильных провайдеров.
*   🛡️ **Имитация реального веб-ресурса (Decoy Page)**: при переходе на ваш домен посторонних лиц или DPI-сканеров, сервер демонстрирует правдоподобный рабочий интерфейс корпоративного портала Confluence (поддерживает автоопределение RU/EN языков браузера).
*   🔑 **Быстрая команда `xry`**: Забудьте длинные пути! Просто введите три буквы: **`xry`** в терминале вашего сервера для мгновенного вызова графической интерактивной панели управления.

---

## 🛠️ Быстрый старт (для новичков)

> [!IMPORTANT]
> Перед запуском убедитесь, что в панели вашего DNS-провайдера (например, Cloudflare) настроена **A-запись**, указывающая домен (например, `vpn.mycoolsite.com`) на публичный IP-адрес вашего сервера.

Выполните одну простую команду в терминале вашего сервера для удаленного запуска установки:

```bash
curl -fsSL "https://raw.githubusercontent.com/mvrvntn/xray-vless-install/main/install_xray.sh?v=$(date +%s)" -o /tmp/install_xray.sh && sudo bash /tmp/install_xray.sh
```

---

## 🖥️ Управление сервером через интерактивную консоль `xry`

После успешного завершения установки, вам больше не нужно запускать скрипты вручную. Просто введите в консоль команду:
```bash
xry
```

Перед вами откроется интерактивный русскоязычный дашборд:

```text
┌────────────────────────────────────────────────────────┐
│  Сервер: sub.domain.com
│  Службы: Xray: [🟢 ACTIVE] | Hysteria 2: [🟢 ACTIVE] | Sub-Server: [🟢 ACTIVE]
│  Обход:  WARP: [🟢 ON (SMART)] | Opera Proxy: [🟢 ON]
│  Клиенты: Активных устройств: 3
└────────────────────────────────────────────────────────┘
┌────────────────────────────────────────────────────────┐
│                      ГЛАВНОЕ МЕНЮ                      │
└────────────────────────────────────────────────────────┘
 1. 📱 Показать QR-коды и ссылки подключения
 2. 👤 Добавить нового пользователя / устройство
 3. 🗑️ Удалить существующего пользователя
 4. 🌀 Управление обходами блокировок (WARP & Opera Proxy)
 5. 📰 Просмотреть системные логи служб
 6. 📊 Мониторинг active-соединений (port 443)
 7. 🛠️ Запустить полную диагностику системы (Troubleshooting)
 8. 🔄 Обновить скрипт с GitHub и применить новые фиксы
 9. 🌐 Изменить отпечаток TLS (Fingerprint)
 10. 🌐 Смена основного домена (SSL)
 11. 🗑️ Полностью удалить всю установку Xray с сервера
 12. 🚪 Выйти из терминала
──────────────────────────────────────────────────────────
Выберите действие (1-12):
```

---

## 🛠️ Дополнительные режимы запуска

### 1. Headless-режим (быстрая установка одной строкой)
Полезно для автоматического развертывания на множестве серверов без лишних диалогов:
```bash
sudo bash install_xray.sh --headless <домен> <email> <кол-во_устройств> [имена_устройств...]
```
*Пример:*
```bash
sudo bash install_xray.sh --headless vpn.mysite.com admin@mail.com 3 "iPhone" "Macbook" "HomePC"
```

### 2. Ручное обновление списков обхода геоблокировок
```bash
sudo bash install_xray.sh --update-geoblocks
```
*(Обычно запускается автоматически планировщиком Cron раз в сутки в 3:30 ночи).*

### 3. Автоматическое обновление ядра и конфигураций (без потери данных)
Если в репозиторий были добавлены новые оптимизации защиты (например, новые методы обхода DPI) или фиксы, вы можете обновить ядро Xray/Hysteria 2 и скрипты подписок, не теряя созданных клиентов (UUID), домен и настройки обходов.

Для этого просто вызовите команду `xry` в терминале и выберите пункт **`8`** (`Обновить скрипт с GitHub и применить новые фиксы`). Скрипт сам скачает последнюю актуальную версию напрямую по хэшу коммита (обходя кэш) и аккуратно обновит все службы.

> [!TIP]
> Если после обновления ссылки в клиенте не изменились, убедитесь что служба Python перезапущена, выполнив вручную: `systemctl restart xray-sub`.

---

## 🤝 Совместная работа рядом с AntiZapret-VPN (на одном сервере)

Вы можете абсолютно безопасно и эффективно использовать этот проект **на одном сервере вместе с AntiZapret-VPN**. Они будут отлично работать в параллели без малейших конфликтов:

### Почему они не мешают друг другу?
*   **AntiZapret-VPN** работает преимущественно по протоколу **UDP** (OpenVPN UDP на порту 1194 и WireGuard на портах 50000+).
*   Наш сервер **Xray** работает исключительно по протоколу **TCP** на портах **443** (для VLESS) и **80** (для выпуска SSL сертификатов).

### 📝 Пошаговая инструкция по параллельной установке:

#### Шаг 1. Освобождение порта 443 TCP в AntiZapret
По умолчанию AntiZapret занимает порт `443 TCP` в качестве резервного для OpenVPN. Чтобы отдать его более скоростному Xray VLESS:
1. Запустите стандартную установку **AntiZapret-VPN**.
2. В процессе установки или после неё остановите и навсегда отключите TCP-версию служб AntiZapret:
   ```bash
   sudo systemctl stop openvpn-server@antizapret-tcp openvpn-server@antizapret-no-vpn-tcp 2>/dev/null
   sudo systemctl disable openvpn-server@antizapret-tcp openvpn-server@antizapret-no-vpn-tcp 2>/dev/null
   ```
   *(При этом весь функционал обхода AntiZapret продолжит безупречно работать через основные протоколы UDP).*

#### Шаг 2. Установка VLESS TCP
Запустите команду установки нашего скрипта:
```bash
curl -fsSL "https://raw.githubusercontent.com/mvrvntn/xray-vless-install/main/install_xray.sh?v=$(date +%s)" -o /tmp/install_xray.sh && sudo bash /tmp/install_xray.sh
```
Встроенный интеллектуальный инсталлятор сам проверит порты, обнаружит любые оставшиеся конфликты служб и поможет безопасно завершить настройку.

##### Шаг 3. Исключение конфликта фаерволов
Сервер AntiZapret использует сложные низкоуровневые правила `iptables NAT` для маршрутизации трафика и перехвата DNS. Системный фаервол **UFW** при активации жестко перекрывает и ломает эти цепочки (пользователи подключаются к OpenVPN, но у них нет интернета).

*Наш интеллектуальный установщик автоматически обнаруживает наличие AntiZapret на сервере, отключает UFW и прописывает разрешающие правила для портов Xray/Hysteria напрямую в iptables.*

Если вы настраивали фаервол вручную или включили UFW после установки:
1. Полностью отключите UFW:
   ```bash
   sudo ufw disable
   ```
2. **Перезагрузите сервер**, чтобы скрипты AntiZapret заново применили свои цепочки правил маршрутизации в `iptables`:
   ```bash
   sudo reboot
   ```
*(Для Xray это абсолютно безопасно, так как он слушает порт 443 напрямую, и базовый фаервол Linux прекрасно с этим справляется).*

---

## 🛠️ Встроенная система диагностики (Troubleshooting)

Если у вас возникли проблемы с подключением, просто введите `xry` и выберите пункт **`7`** (`Запустить полную диагностику системы`).
Скрипт автоматически выполнит следующие проверки и покажет детальные результаты:
1.  **Сетевые порты 443 и 80**: проверит, какие именно процессы заняли данные порты и нет ли там конфликтов с OpenVPN/Nginx.
2.  **Демоны**: выведет статус активности служб `xray` и `xray-sub`.
3.  **DNS & Домен**: проверит локальный резолвинг домена и предупредит, если IP-адрес подменяется локальной заглушкой AntiZapret (`dnsmap` из подсети `10.224.x.x`).
4.  **Состояние WARP**: проверит целостность WireGuard-интерфейса и выполнит тестовый curl-запрос через WARP к Cloudflare для проверки доступности.
5.  **Сертификаты SSL**: проверит физическое наличие сертификатов Let's Encrypt и укажет точную дату окончания их действия.

---

## 📂 Структура проекта на сервере

После установки все ключевые компоненты распределяются по стандартным системным путям:

*   💾 **Конфигурационный файл Xray**: `/usr/local/etc/xray/config.json`
*   👥 **Конфиги пользователей**: `/etc/xray/client_configs/*.json`
*   🔑 **Быстрый ярлык**: `/usr/local/bin/xry` (ссылается на `/etc/xray/install_xray.sh`)
*   ⚙️ **Сервер подписок (Python)**: `/usr/local/bin/xray_sub_server.py`
*   🔄 **Демон systemd подписок**: `/etc/systemd/system/xray-sub.service`
*   📅 **Авто-обновление обходов (Cron)**: `/etc/cron.d/xray-geoblock-update`
*   📝 **Журналы событий (Логи)**: `/var/log/xray/`

---

## 🛑 Полное удаление проекта с сервера
Если вы решите удалить Xray, просто запустите `xry` и выберите пункт **`11`** (`Полностью удалить всю установку Xray с сервера`). Скрипт деликатно удалит все созданные файлы, службы, cron-задачи и вернет систему в исходное состояние, не затронув другие настройки сервера.
