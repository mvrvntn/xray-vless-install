# Plan: `/graphify --update` on vless_server_instal

## Context

- Existing graph: **6208 nodes, 10091 edges, 747 communities** (built from commit `43747b1`)
- Current HEAD: `24b4ffb` — 2 commits ahead: `cd3f731` + `24b4ffb`
- Both commits modify **`install_xray.sh`** only (~3050 lines bash script)
- `.graphify_python` / `.graphify_root` exist → no interpreter re-resolution needed
- Graphify v0.7.10, Python: `python` (not `python3` on Windows)
- No `MOONSHOT_API_KEY` → default model for semantic extraction

## Pipeline (--update path)

### Step 1: Interpreter guard
✅ `.graphify_python` exists — skip re-resolution.

### Step 2: Detect incremental changes
```bash
python -c "from graphify.detect import detect_incremental; ..." > .graphify_incremental.json
```
Expected: 1 file changed (`install_xray.sh`), 0 deleted, possibly new untracked files.

### Step 3: Code-only check
`.sh` is NOT in graphify's `code_exts` set → `code_only = False`
→ Full extraction pipeline needed (AST + semantic) on changed files.

### Step 4: AST extraction (Step 3A)
Only on code files in changed set (likely none or few).

### Step 5: Semantic extraction (Step 3B)
LLM-based extraction on `install_xray.sh` (and any other changed non-code files).
- Single file → 1 chunk → 1 subagent call.
- Subagent type: `general-purpose` (must write `.graphify_chunk_N.json` to disk).

### Step 6: Merge (Step 3C)
Merge AST + semantic → `.graphify_extract.json`

### Step 7: Merge into existing graph
Load `graph.json` → merge new nodes/edges → prune deleted files → save manifest.

### Step 8: Re-cluster & analyze (Step 4)
Rebuild graph, re-run community detection, compute god nodes + surprising connections.

### Step 9: Label communities (Step 5)
Review communities, assign 2-5 word names, regenerate report with labels.

### Step 10: Generate outputs (Step 6)
- `graph.html` — interactive visualisation  
- `GRAPH_REPORT.md` — updated report  
- `graph.json` — merged data

### Step 11: Token benchmark (Step 8, only if words > 5000)
Corpus is ~212k words → run `graphify benchmark`.

### Step 12: Cleanup & manifest (Step 9)
Save manifest, update `cost.json`, remove temp files.

## Output
```
Graph complete. Outputs in C:\Users\mvrvntn\Desktop\PROJECTS\vless_server_instal\graphify-out\
  graph.html        - interactive graph
  GRAPH_REPORT.md   - audit report
  graph.json        - raw graph data
```

## Post-pipeline
- Extract God Nodes, Surprising Connections, Suggested Questions from report
- Offer to explore most interesting cross-community question
